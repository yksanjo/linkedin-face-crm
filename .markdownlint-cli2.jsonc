{
  // Lint config for markdownlint-cli2 (used by CI and locally).
  // Default rules apply in full; this file only scopes which paths are linted.
  "ignores": [
    "node_modules/**",
    ".next/**"
  ]
}
